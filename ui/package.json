{
  "name": "@cuttio/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "cutti editor UI — a Vite/React surface (openscreen stack) that drives the cutti daemon's editing engine over HTTP. project.json is the single source of truth.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.9.2",
    "vite": "^5.4.11"
  }
}
